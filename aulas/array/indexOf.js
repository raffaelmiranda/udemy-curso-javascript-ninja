var arr = [1, 2, 3, 4, 5];

console.log(arr.indexOf(3)); //retorna o índice 2 do elemento encontrado
console.log(arr.indexOf(6)); //retorna -1 quando o elemento não é encontrado
console.log(arr.indexOf(3, 3)); //retorna -1 quando o elemento não é encontrado a partir do índice especificado
